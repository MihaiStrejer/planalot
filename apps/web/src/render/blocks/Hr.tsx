import React from "react";
import type { BlockProps } from "../registry";

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function Hr(_props: BlockProps): React.ReactElement {
  return <hr />;
}
